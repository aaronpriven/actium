require ('/home/priven/public_html/cgi-lib.pl');&ReadParse;# get data from form$linenum = $in{"linenum"};$dirname = $in{"dirname"};$direction = $in{"direction"};$totallines = $in{"totallines"};$pickedpoint = $in{"pickedpoint"};$offset = $in{"offset"};$pickedpoint--;@alllines = split(":", $in{"alllines"};if (not $in{"checkline"} and $#allines# you pick a 1-based number, but the program needs a 0-based numberif ($in{"checkline"}) {    $linesflag = 1;    @lines = sort (split("\0", $in{"checkline"}));    if ($#lines > 1 ) {        $linesstring = "Lines ";        foreach (0 .. $#lines - 1) {            $linesstring .= "$lines[$_], ";        }        $linesstring .= "and $lines[$#lines]";    } elsif ($#lines == 1) {        $linesstring = "Lines $lines[0] and $lines[1]";    } else {        $linesstring = "Line $lines[0]";    }} else {   $linesstring = "Line $linenum";
#!/usr/bin/perl

foreach (sort glob('*.eps')) {

   next if /\.outl\.eps$/;
   my $file = $_;
   $file =~ s/\.eps/.outl.eps/;
   if (-e $file) {
      $label = 'None';
   }
   else {
      $label = 'Yellow';
   }

   #print "$label $_\n";
   system "/usr/local/bin/setlabel $label $_";

}
